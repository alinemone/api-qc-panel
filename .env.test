# Test .env file - for local testing without real database

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Database (fake - just for config)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=test
POSTGRES_USER=test
POSTGRES_PASSWORD=test
POSTGRES_SCHEMA=call

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# JWT
JWT_SECRET_KEY=test-secret-key-for-local
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
